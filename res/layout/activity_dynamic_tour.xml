<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".DynamicTour" >

    
      <TextView
       	android:id="@+id/number_poles"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:text="@string/choose_poles"/>

    <Spinner
    	android:id="@+id/poles_spinner"
    	android:layout_width="match_parent"
    	android:layout_height="wrap_content"
        android:entries="@array/poles_array"
        android:drawSelectorOnTop="true"
        android:spinnerMode="dialog"
       	android:layout_below="@id/number_poles"
        android:prompt="@string/poles_prompt" />
       	

    
      <TextView
       android:id="@+id/level"
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"       
       android:layout_below="@id/poles_spinner"
       android:textSize="20sp"
       android:text="@string/choose_level" />
    
     <Spinner
    	android:id="@+id/level_spinner"
    	android:layout_width="match_parent"
    	android:layout_height="wrap_content"
        android:entries="@array/level_array"
        android:drawSelectorOnTop="true"
       	android:spinnerMode="dialog"
       	android:layout_below="@id/level"       	
        android:prompt="@string/choose_level" />
     
     <Button
       android:id="@+id/btnSubmit"
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       android:layout_below="@id/level_spinner"
       android:text="Submit" />

   

</RelativeLayout>
